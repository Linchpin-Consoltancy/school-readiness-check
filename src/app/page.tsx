import { Experience } from "@/components/experience/Experience";

/**
 * The whole tool. One address, one screen, no reloads. Everything the
 * director moves through happens inside the frame in Experience.
 */
export default function HomePage() {
  return <Experience />;
}
